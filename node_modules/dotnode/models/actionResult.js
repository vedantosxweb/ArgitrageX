var resultTypes = require('../models/actionResultType');

var ActionResult = function (type, data) {

    this.type = type;
    this.data = data;
};

ActionResult.prototype.type = resultTypes.viewResult;

ActionResult.prototype.data = {};

module.exports = ActionResult;